---
title: AI Recommended Assets
description: Data-driven ranking + AI interpretation for top-200 Bithumb KRW market assets.
openapi: openapi.json GET /v1/market/ai-recommended-assets
---

<div style={{ maxWidth: 760 }}>

<h2 style={{ fontSize: "22px", marginBottom: 8 }}>Overview</h2>

<p style={{ fontSize: 15, lineHeight: 1.8 }}>
  The <strong>AI Asset Recommendation</strong> API provides a data-driven ranking and evaluation of crypto assets
  listed in the <strong>Bithumb KRW</strong> market. It returns the <strong>top 200 tokens by market cap</strong> by default,
  combining market data with multi-dimensional scoring and AI-generated interpretation.
</p>

<Note>
  This endpoint returns a point-in-time snapshot. If <code>date</code> is omitted, the most recent snapshot is returned.
</Note>

<h3 style={{ marginTop: 20 }}>Variants</h3>

<p style={{ fontSize: 15, lineHeight: 1.8 }}>
  Two additional endpoints return subsets of the same data:
</p>

<ul style={{ fontSize: 15, lineHeight: 1.8 }}>
  <li><code>/v1/market/ai-recommended-assets-top50</code> — top 50 by market cap (rank 1–50)</li>
  <li><code>/v1/market/ai-recommended-assets-bottom50</code> — bottom 50 of top 200 (rank 151–200)</li>
</ul>

<p style={{ fontSize: 15, lineHeight: 1.8 }}>
  All three endpoints accept the same parameters and return the same response structure.
</p>

<h3 style={{ marginTop: 20 }}>Two core components</h3>

<ul style={{ fontSize: 15, lineHeight: 1.8 }}>
  <li>
    <strong>Recommendation List</strong>: ranked assets based on a composite <code>Total Score</code> (0–10),
    computed as the rounded average of six category scores.
  </li>
  <li>
    <strong>Recommendation Details</strong>: in-depth evaluation for each asset, including score interpretation,
    category breakdown, price context, and social sentiment distribution.
  </li>
</ul>

<h3 style={{ marginTop: 20 }}>Scoring model</h3>

<div style={{ fontSize: 15, lineHeight: 1.8 }}>
  <p style={{ marginBottom: 8 }}>
    Total Score is derived from six categories (each 0–10):
  </p>

  <ul>
    <li><strong>Upside Energy</strong></li>
    <li><strong>Trading Activity</strong></li>
    <li><strong>Search Trend</strong></li>
    <li><strong>Price Stability</strong></li>
    <li><strong>Market Sentiment</strong></li>
    <li><strong>Price Resilience</strong></li>
  </ul>

  <p style={{ marginTop: 10 }}>
    The response includes <code>score_by_category</code> and, for comprehensive evaluations,
    <code>interpretation</code> labels per category.
  </p>
</div>

<h3 style={{ marginTop: 20 }}>How to use</h3>

<Steps>
  <Step title="Call without tickers (default)">
    Omit <code>tickers</code> to receive data for <strong>all top-200 tokens</strong> sorted by score.
  </Step>

  <Step title="Or filter by specific tickers">
    Pass <code>tickers</code> as a comma-separated list (e.g. <code>BTC,ETH,SOL</code>).
    Only tokens within the market cap range of this endpoint are returned.
  </Step>

  <Step title="Choose a response type">
    Set <code>type</code> (required) to one of: <code>score_ranking</code>, <code>summary</code>,
    <code>comprehensive_evaluation</code>, <code>sentiment_trend</code>.
  </Step>

  <Step title="Optional: request a historical snapshot">
    Provide <code>date</code> (YYYY-MM-DD). The backend treats it as KST 00:00 for that day.
  </Step>

  <Step title="Read the snapshot">
    The response includes <code>generated_at</code> (KST) and an <code>assets</code> list.
  </Step>
</Steps>

<Warning>
  Unknown or out-of-range tickers are omitted. If certain data is unavailable, corresponding fields may be
  <code>null</code> or omitted.
</Warning>

</div>
